A simple implementation of Othello using the command line.

First you will input your chosen difficulty between 1 and 10, this decides how far down the tree the AI looks

then you will decide if you want to go first or second

from there, your inputs will be as XY coordinates it will be in the form of "X Y" 

You will battle against the AI which uses a minimax algorithm with alpha beta pruning 